# Colab Packages
# evals dependencies
# Core ML/AI Framework
sentence-transformers>=5.1.0

# Distributed Computing
ray>=2.51.0
ray[default]>=2.51.0

# LLM Inference
# transformers>=4.56.1,<5.0.0
transformers==4.56.1

# OpenAI API
openai>=1.106.1
tiktoken>=0.12.0

# LangChain Ecosystem
langchain>=1.0.5
langchain-classic>=1.0.0
langchain-core>=1.0.4
langchain-community>=0.4.1
langchain-openai>=1.0.2
langchain-huggingface>=1.0.0
langchain-pinecone>=0.2.13
langchain-postgres>=0.0.17

# Data Manipulation & Display
unstructured>=0.18.15

# Other
requests==2.32.5
vllm==0.11.0
faiss-gpu-cu12
fsspec>=2023.1.0,<=2025.3.0
# flashinfer 0.3.1 is the version vLLM 0.11.0 (pip) expects.
# Do NOT use >=0.5.x: it introduces CUTLASS CuTE DSL that JIT-compiles at import
# time inside vLLM's MP subprocess (before CUDA is initialized), causing NVVM
# errors on all GPUs. 0.5.x also requires sm_80+ (T4 is sm_75).
flashinfer-python==0.3.1

# MLflow dashboard dependencies
mlflow>=3.2.0
gunicorn>=23.0.0
flask-cors>=5.0.1

# Logging
loguru


numpy==2.0.1